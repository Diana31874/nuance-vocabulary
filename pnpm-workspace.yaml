packages:
  - .

# These packages ship native binaries used by the Next.js/Vite/Cloudflare build.
# pnpm 11 blocks dependency lifecycle scripts unless they are explicitly trusted.
allowBuilds:
  esbuild: true
  sharp: true
  unrs-resolver: true
  workerd: true
